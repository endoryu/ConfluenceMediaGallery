name: fixture
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npx forge install --upgrade --confirm-scopes --site example.atlassian.net --product confluence -e development
